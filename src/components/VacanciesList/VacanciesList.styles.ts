import { createStyles } from '@mantine/core';

const useStyles = createStyles(() => ({
  container: {
    padding: 50,
  },

  button: {
    fontWeight: 600,
    fontSize: 14,
    borderRadius: 8,
  },
}));

export default useStyles;
